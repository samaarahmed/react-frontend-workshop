import React, { useState } from 'react';

const Skill = () => {

    const intialSkills = ['Java','JavaScript','React']
    const [skill,setSkills] = useState(intialSkills);

    return (
        <div>
            
        </div>
    );
};

export default Skill;